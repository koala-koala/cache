今天分享得主题是浏览器缓存，浏览器缓存就是一种请求时保存资源副本到客户端待下次请求时直接使用该副本的技术，
我们的网站中都有用到这项技术，以优矿首页为例，在清除缓存访问页面的情况下，页面加载较慢，开启缓存，性能得到明显
的提升。浏览器缓存可以减少重复的请求，减少传输内容，降低服务器压力。
浏览器缓存存储位置：/Users/fu.zhang/Library/Caches/Google/Chrome/Default/Cache

浏览器对于请求资源拥有一系列成熟的缓存策略，按照发生的时间顺序分为存储策略、过期策略、协商策略，
其中存储策略再收到响应后应用，过期策略和协商策略在发送请求前应用。
我们先来看一下浏览器缓存策略的流程图：
客户端发起请求，浏览器判断是否有缓存，如果没有缓存，则会直接向服务器请求资源，浏览器会把响应资源实体和响应头存入
缓存；如果有缓存，浏览器会判断缓存是否过期，如果没有过期则命中强缓存，浏览器直接从缓存中读取资源副本；如果缓存过期
了，则向服务器发送请求校验资源新鲜度，资源没有更新，命中协商缓存，服务器返回304，浏览器直接从缓存中读取资源副本，
如果资源有更新，则向服务器发送请求请求资源。
我们可以看到这个流程图中有三次判断的过程，那决定这些判断的字段有哪些可以看一下这个表格。